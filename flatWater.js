class FlatWater {
    constructor(size, surface) {
        this.size = size;
        this.surface = surface;
        this.coords = [0, 0, 300, 0, 0, 0, 300, 0, 300, 300, 0, 0];
        
        // if (size > 0) {
        //     this.size = size / 2;
        // }
        // else throw new Error("size of cube must be more than 0");
        // this.x = x;
        // this.y = y;
        // this.z = z;
        // this.color = color || [0.1, 0.9, 0.6, 1];


        // let arr = [];
        // for (let i = 0; i < array.length; i += 3) {
        //     let newCube = new Cube(size, array[i], array[i + 1], array[i + 2]);
        //     let newArr = newCube.cubeCoords;
        //     arr = arr.concat(newArr);
        // }
        // this.waterCoords = arr;

        // this._create();
    }
}

